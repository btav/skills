interface:
  display_name: "btav pr body"
  short_description: "Draft concise PR descriptions"
  default_prompt: "Use $btav-pr-body to draft a concise GitHub PR body from the current diff."

policy:
  allow_implicit_invocation: false
